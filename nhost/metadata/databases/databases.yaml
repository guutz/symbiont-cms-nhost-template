sources:
  - name: default
    kind: postgres
    tables: !include tables/tables.yaml
    configuration:
      connection_info:
        database_url:
          from_env: HASURA_GRAPHQL_DATABASE_URL
        isolation_level: read-committed